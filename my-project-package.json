{
  "name": "my-project",
  "version": "1.0.0",
  "description": "A Node.js + React application with MongoDB and Telegram bot",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "client:build": "cd client && npm install && npm run build",
    "build:all": "npm install && npm run client:build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "telegraf": "^4.12.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}